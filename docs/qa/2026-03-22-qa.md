# QA 驗收報告 — Phase 3.2

> **報告日期**：2026-03-22
> **QA 執行者**：dispatch worker（qa-check 任務，claude-sonnet-4-6）
> **對照規格**：
> - `docs/specs/intake-module.md` v1.1
> - `docs/specs/dispatch-module.md` v1.1
> - `docs/specs/sdd-workflow.md` v1.1
> **檢查範圍**：`src/modules/intake/`、`src/modules/dispatch/`

---

## 一、intake-module.md (v1.1) — 驗收結果

| AC-ID | 條件摘要 | 結果 | 說明 |
|-------|---------|------|------|
| AC-01 | 輸入有效需求描述後，`docs/intake/<slug>.md` 被創建 | [pass] | `runIntake()` 在 `parser.ts` 中呼叫 `buildIntakeSlug()` + `writeFile()`，檔案正確寫入 |
| AC-02 | 輸出文件包含 YAML frontmatter 及七個章節 | [pass] | `template.ts` 的 `buildMarkdown()` 生成含 `project_name/version/created_at/intake_slug` 的 frontmatter，及七個 `## 一~七` 章節 |
| AC-03 | MVP 功能清單至少 1 個項目，每個項目含 ID/名稱/說明/Phase | [fail] | `normalizeFeatures()` 未驗證 `mvp_features` 最少需有 1 個項目；AI 回傳空陣列時，文件仍被寫入（MVP 章節為空） |
| AC-04 | 風險清單至少 1 個項目，含嚴重度與緩解策略 | [fail] | `normalizeRisks()` 未驗證 `risks` 最少需有 1 個項目；AI 回傳空陣列時，文件仍被寫入（風險章節為空） |
| AC-05 | 缺少 `project_name` 或 `description` 時，API 回傳 400 | [pass] | `api.ts` 分別驗證兩個欄位，回傳 400 + `"project_name and description are required"` |
| AC-06 | 相同 `project_name` 重複提交時，舊文件直接被覆蓋 | [pass] | `parser.ts` 使用 `buildIntakeSlug()`（無碰撞後綴），同 project_name → 同 slug → 直接覆蓋；code 有明確 `// AC-06` 注解 |
| AC-07 | dispatch "intake" 後，YAML frontmatter 可被 write-spec 直接解析 | [pass] | frontmatter 含 `project_name`、`intake_slug`、`version`、`created_at`，格式符合 intake-module.md § 3.4 與 RV-025 定義 |
| AC-08 | AI 解析失敗時，輸出明確錯誤訊息且不寫入空白文件；最多重試 2 次（共 3 次） | [pass] | `CONFIG.ai.totalAttempts = 3`；`parser.ts` 的迴圈最多執行 3 次；僅在成功後呼叫 `writeFile()`；失敗時拋出 `AIParseError` |
| AC-09 | CLI 觸發時 project_name 無法提取，透過 IPC 詢問；3 分鐘無回應使用時間戳 fallback | [fail] | `IntakeInput.project_name` 在介面層為必填；`parser.ts` 中無三段式提取流程（AI 提取 → IPC → 時間戳 fallback）。規格 § 4.3 要求此邏輯在 `parser.ts` 內實作，但目前完全未實作 |

**小計**：[pass] 6 / [fail] 3 / [skip] 0

---

## 二、dispatch-module.md (v1.1) — 驗收結果

| AC-ID | 條件摘要 | 結果 | 說明 |
|-------|---------|------|------|
| AC-01 | dispatch "write-spec: agency-os" 後，任務目錄與 `plan.md` 被建立 | [pass] | `createTask()` 呼叫 `renderTemplate()` + `writeFile(planPath, rendered)`，目錄與 `plan.md` 正確建立 |
| AC-02 | 任務啟動後，狀態從 `pending` 轉為 `running` | [pass] | `api.ts` 的 POST /api/dispatch：`createTask()`（pending）→ 立即 `updateTaskStatus(task.id, "running")` |
| AC-03 | Worker 完成所有 checklist 後，plan.md 全為 [x]，output.md 被寫入，狀態 → done | [skip] | 此為 Claude agent worker 的行為測試，非模組 API 測試；`markItemDone()`、`updateTaskStatus("done")`、`writeFile()` 等基礎設施均已實作 |
| AC-04 | Worker 提問後，IPC 目錄出現 `001.question`，狀態 → `waiting_input`，`context.md` 被寫入 | [fail] | `001.question` 寫入 ✓（`askAndPause()` 正確原子寫入）；狀態 → waiting_input ✓（`handleWaitingForInput()` 設定）；**`context.md` 未寫入磁碟** ✗ — `handleWaitingForInput()` 僅將 context 存入記憶體的 TaskRecord，不寫 `context.md`；`context.md` 只有在 DELETE 停止任務時才寫入（`stopTask()`） |
| AC-05 | 用戶寫入 `001.answer`，重新觸發 worker，worker 繼續執行，狀態 → `running` | [skip] | 端對端 agent 行為測試；infrastructure（writeAnswer + checkPendingAnswer + updateTaskStatus）均實作；規格允許「手動重新觸發」 |
| AC-06 | 提問後 deadline 過期，下次觸發偵測逾時，task 狀態 → `timed_out` | [pass] | GET /api/dispatch/tasks/:id handler：當 task 為 `waiting_input` 時，取得 `pending_question.deadline` 並與當前時間比較；若逾時呼叫 `updateTaskStatus(taskId, "timed_out")`；`isQuestionTimedOut()` 亦實作於 ipc-manager.ts |
| AC-07 | POST /api/dispatch/tasks/:id/answer 成功後，`.answer` 文件原子寫入且非空 | [pass] | `ipc-manager.ts` 的 `writeAnswer()` 先驗證非空（拋 Error），再呼叫 `atomicWrite()`（temp → rename）；API 層回傳 400 if empty |
| AC-08 | 不存在的 alias 使用時，API 回傳 400 並列出有效 alias 清單 | [pass] | `command-parser.ts` 呼叫 `getAliasEntry()` → 拋 `InvalidAliasError`（含有效清單）；`api.ts` catch 後回傳 400 + message |
| AC-09 | 2 個任務同時執行時，各自 plan.md 和 ipc/ 目錄互不干擾 | [skip] | 架構隔離靠 slug 唯一目錄確保；需並行執行整合測試驗證，非模組單元測試範疇 |
| AC-10 | GET /api/dispatch/tasks 回傳全部任務及六項狀態統計 | [pass] | `api.ts` 的 `statusCounts()` 計算 running/done/failed/timed_out/waiting_input/pending 六項；GET handler 合併回傳 |

**小計**：[pass] 6 / [fail] 1 / [skip] 3

---

## 三、sdd-workflow.md (v1.1) — 驗收結果

> **前置發現**：規格要求的 `src/modules/sdd/` 模組完全未實作。以下所有 AC 皆因基礎設施缺失而 [fail]。

| AC-ID | 條件摘要 | 結果 | 說明 |
|-------|---------|------|------|
| AC-01 | implement 時規格文件不存在 → Spec Gate 失敗，任務停止 | [fail] | `src/modules/sdd/spec-gate.ts` 未建立；Spec Gate 機制完全未實作 |
| AC-02 | 規格版本 v1.0 時 Spec Gate 失敗，提示需先 review-spec + update-spec | [fail] | 同上 |
| AC-03 | 規格缺少必要章節時 Spec Gate 失敗並列出缺失章節 | [fail] | 同上；`spec-parser.ts` 未建立 |
| AC-04 | review 文件不存在時 Spec Gate 失敗，提示先執行 review-spec | [fail] | 同上 |
| AC-05 | review 文件含 OPEN 問題時 Spec Gate 失敗並列出問題 ID | [fail] | 同上；`review-parser.ts` 未建立 |
| AC-06 | update-spec 後版本號自動遞增並加入 changelog | [fail] | `src/modules/sdd/version-manager.ts` 未建立 |
| AC-07 | 所有問題 RESOLVED 後，Spec Gate open_issues 檢查通過 | [fail] | spec-gate.ts 未建立 |
| AC-08 | GET /api/sdd/status 正確反映所有 Phase 狀態 | [fail] | `src/modules/sdd/api.ts` 未建立；`/api/sdd/*` 路由不存在 |
| AC-09 | drift-check API 識別實作不符 AC，回傳 analysis_method 與 coverage_note | [fail] | `src/modules/sdd/drift-checker.ts` 未建立 |
| AC-10 | Phase 1 完成後，所有規格 ≥ v1.1、所有 review 問題 RESOLVED、Spec Gate 通過 | [fail] | 端對端流程依賴 sdd 模組，完全未實作 |
| AC-11 | write-spec 生成的規格文件包含所有 5 個必要章節 | [skip] | 行為測試（write-spec worker 輸出），非程式碼模組可驗證範疇 |
| AC-12 | 傳入 require_reviewed=false 但 alias 為實作類時，強制以 true 執行 | [fail] | Spec Gate 未實作，此邏輯不存在 |

**小計**：[pass] 0 / [fail] 11 / [skip] 1

---

## 四、整體統計

| 模組 | [pass] | [fail] | [skip] | 總計 |
|------|--------|--------|--------|------|
| intake-module | 6 | 3 | 0 | 9 |
| dispatch-module | 6 | 1 | 3 | 10 |
| sdd-workflow | 0 | 11 | 1 | 12 |
| **合計** | **12** | **15** | **4** | **31** |

---

## 五、[fail] 項目修復優先順序

### P0 — 阻擋後續開發（必須先修復）

**F1. sdd-module 整模組未實作**
- 影響：sdd-workflow AC-01 ~ AC-10, AC-12（11 條 AC 全部失敗）
- 說明：`src/modules/sdd/` 目錄不存在。需建立 `spec-gate.ts`、`spec-parser.ts`、`review-parser.ts`、`version-manager.ts`、`drift-checker.ts`、`api.ts`，並掛載 `/api/sdd/*` 路由。
- 對應規格：sdd-workflow.md § 4.3, § 4.4, § 4.5, § 4.2

### P1 — 核心功能缺失（影響 dispatch worker 正確執行）

**F2. dispatch AC-04：context.md 未在 waiting_input 轉換時寫入磁碟**
- 影響：dispatch AC-04
- 說明：`handleWaitingForInput()` 只將 context 存入記憶體 TaskRecord，不寫 `context.md`。規格要求 worker 暫停時立即寫入以便重啟恢復。需在 `handleWaitingForInput()` 或 `askAndPause()` 中加入磁碟寫入邏輯。
- 對應規格：dispatch-module.md § 3.4, § 4.4

**F3. intake AC-09：parser.ts 缺少 project_name 三段式提取流程**
- 影響：intake AC-09
- 說明：`parser.ts` 元件設計說明包含「project_name 提取邏輯」，但實作中 `IntakeInput.project_name` 為必填，無 AI 提取、IPC 詢問、時間戳 fallback 三個步驟。需在 `parser.ts` 中加入提取邏輯，或調整 dispatch worker 的 plan 模板來承接此邏輯。
- 對應規格：intake-module.md § 3.1, § 3.3, AC-09

### P2 — 資料完整性驗證缺失（不會崩潰，但輸出品質不可控）

**F4. intake AC-03：mvp_features 無最少 1 個項目驗證**
- 影響：intake AC-03
- 說明：`normalizeFeatures()` 在 AI 回傳空 mvp_features 時不拋錯，直接寫入空 MVP 章節的文件。需在 `runIntake()` 或 `normalizeFeatures()` 後加入最少數量驗證，若為空則觸發重試或報錯。
- 對應規格：intake-module.md AC-03

**F5. intake AC-04：risks 無最少 1 個項目驗證**
- 影響：intake AC-04
- 說明：`normalizeRisks()` 在 AI 回傳空 risks 時不拋錯，直接寫入空風險章節的文件。需加入最少數量驗證。
- 對應規格：intake-module.md AC-04

---

## 六、備注

- **[skip] 項目說明**：dispatch AC-03, AC-05, AC-09 及 sdd-workflow AC-11 屬於 Claude agent worker 的端對端行為測試，依賴完整 dispatch 工作流執行，無法透過單元或 API 測試驗證，基礎設施均已實作。
- **sdd-workflow 優先級**：整個 sdd 模組為 Phase 2+ 功能，目前 Phase 2 核心實作已完成（118/118 tests pass）。sdd 模組應在 Phase 3.1+ 規劃中實作。
- **測試覆蓋**：目前 118 個通過的測試針對 intake/dispatch 模組的已實作功能；sdd 模組無任何測試。

---

*此 QA 報告由 dispatch worker（qa-check 任務）自動生成。如有修訂請更新並說明原因。*
